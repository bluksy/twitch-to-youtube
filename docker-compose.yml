services:
  app:
    build: .
    restart: 'unless-stopped'
    logging:
        driver: "json-file"
        options:
            max-size: "100m"
            max-file: "5"
    volumes:
      - ./.env:/app/.env
      - ./auth:/app/auth
      - ./youtube_token_check.sh:/app/youtube_token_check.sh
